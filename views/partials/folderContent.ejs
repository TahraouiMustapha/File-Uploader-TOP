<ul>
    <%userFiles.forEach(file => {%>
        <%if( file.folderid ){%>
            <li><a href=<%=`/folders/${file.folderid}`%>><%= `${file.name}, ${file.size}, ${file.createdDate}` %></a></li>
        <%} else { %>
            <li><%= `${file.name}, ${file.size}, ${file.createdDate}` %></li>
        <%}%>                                    
    <%})%>                            
</ul>
                    